Library Dependencies

GCC 2.x / 6.x (m68k-amigaos) can be compiled using different run-time and/or static libraries:

0. base
    bin/ar|as|gcc|ld|nm|ranlib|strip
    lib/ldscripts/amiga*.*

1. ixemul (no arguments)
    lib/crt0
    lib/libc
    lib/libamiga
    lib/libstubs

2. libnix (-noixemul|-mcrt=nix13|-mcrt=nix20)
    lib/libamiga
    lib/libstubs
    libnix/lib/ncrt0
    libnix/lib/libnix20
    libnix/lib/libnixmain
    libnix/lib/libnix

3. clib2 (-mcrt=clib2)
    clib2/ncrt0
    clib2/libc
    clib2/libamiga
    clib2/libdebug

GCC 12.x (m68k-amiga-elf) does not have these switches

Standard C Library = libc, based on glibc (which does not compile for m68k) or newlib (?) or specific other C-libs made for Amiga (libnix/clib2)




GNU "internal" 
